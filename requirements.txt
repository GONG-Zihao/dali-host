# Default installation uses the minimal core dependencies.
-r requirements.base.txt

# Optional components (transports, analytics, services) are listed in:
#   requirements.extras.txt
# Install with: pip install -r requirements.extras.txt
